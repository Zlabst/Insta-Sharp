@model Post

@using (Html.BeginForm("Add", "Comment", FormMethod.Post, new { @class = "comment-form" }))
{
    @Html.AntiForgeryToken()
    @Html.Hidden("postId", @Model.Id)

    <input type="text" name="comment" id="comment" placeholder="Add a comment..." class="no-border" />
}